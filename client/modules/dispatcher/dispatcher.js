/**
 * Module dependencies
 */

var Dispatcher = require('barracks');
var dispatcher = Dispatcher();

/**
 * Expose 'Dispatcher()'
 */

module.exports = dispatcher;

/**
 * Register callbacks.
 */

// dispatcher.register()